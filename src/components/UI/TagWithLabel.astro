---
interface Props {
  label: string;
  icon: any;
  variant?: "social" | "tech";
}

const { label, icon: Icon } = Astro.props;
---

<div
  class="flex justify-center text-[var(--color-primary-dark)] items-center gap-1 px-2 py-1 bg-[var(--color-primary-dark)]/10 rounded-full transition-all duration-200 pointer-events-none shadow-sm border border-[var(--color-primary-dark)]/20"
>
  <Icon extraClasses="w-5 h-5" />
  <span class="text-sm">{label}</span>
</div>
